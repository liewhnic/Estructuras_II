For run this simulation with the default mem_traces you must run in a command shell:
	make compile
or you can just run:
	make

If you want to run the simulation with the test files, you must run:
	make test
	
If you want to delete all the autogenerated files, you must run:
	make clean
